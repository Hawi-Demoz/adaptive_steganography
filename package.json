{
  "private": true,
  "name": "adaptive_steganography_root",
  "version": "0.0.0",
  "scripts": {
    "build": "cd web/frontend && npm ci && npm run build",
    "dev": "cd web/frontend && npm install && npm run dev"
  }
  ,"engines": {
    "node": ">=20.19.0",
    "npm": ">=10"
  }
}
